import 'package:flutter/material.dart';

class ContactChat extends StatelessWidget {
  const ContactChat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
