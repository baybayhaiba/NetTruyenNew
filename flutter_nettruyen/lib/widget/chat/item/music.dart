import 'package:flutter/material.dart';

class MusicChat extends StatelessWidget {
  const MusicChat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
