import 'package:flutter/material.dart';

class FileChat extends StatelessWidget {
  const FileChat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
